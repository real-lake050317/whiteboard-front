<!-- <script lang="ts">
  import { authStore } from "$lib/stores";
  import { checkAuth } from "$lib/api/auth";
  import { browser } from "$app/env";

  if (browser) {
    checkAuth($authStore);
  }
</script> -->
